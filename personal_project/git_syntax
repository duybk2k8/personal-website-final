# 1. Chuyển sang main
git switch main
git pull origin main

# 2. Tạo branch mới
git switch -c /*tên branch*/

# 3. Code...
git add .
git commit -m "/*Comment*/"
git push -u origin /*tên branch*/

# 4. Merge vào main
git switch main
git pull origin main
git merge /*tên branch*/

# 5. Đẩy main
git push origin main

# 6. Xóa branch
git branch -d /*tên branch*/
git push origin --delete /*tên branch*/

#từ Cách 3 thực hiện trực tiếp trên GitHub
#4.1 Lấy code lại và về main
git switch main
git pull origin main